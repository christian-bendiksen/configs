bind=SUPER,Return,spawn,kitty
bind=SUPER,r,reload_config

bind=SUPER,a,spawn,qs -c noctalia-shell ipc call launcher toggle
bind=SUPER,grave,spawn,qs -c noctalia-shell ipc call launcher clipboard

bind=SUPER+Shift,m,spawn,qs -c noctalia-shell ipc call sessionMenu toggle
# bind=SUPER+Shift,l,spawn,qs -c noctalia-shell ipc call lockScreen lock
bind=SUPER,q,killclient,

bind=SUPER,Tab,focusstack,next
bind=SUPER,H,focusdir,left
bind=SUPER,L,focusdir,right
bind=SUPER,K,focusdir,up
bind=SUPER,J,focusdir,down
bind=SUPER+SHIFT,H,exchange_client,left
bind=SUPER+Shift,J,exchange_client,down
bind=SUPER+Shift,K,exchange_client,up
bind=SUPER+Shift,L,exchange_client,right

bind=SUPER,g,toggleglobal,
bind=ALT,Tab,toggleoverview,
bind=SUPER+SHIFT,f,togglefloating,
bind=SUPER,m,togglemaximizescreen,
bind=SUPER,f,togglemaximizescreen,
bind=ALT+SHIFT,f,togglefullscreen,
bind=SUPER,i,minimized,
bind=SUPER+SHIFT,I,restore_minimized
bind=ALT,z,toggle_scratchpad
bind=SUPER,comma,scroller_stack,right
bind=SUPER,period,scroller_stack,left
bind=SUPER,t,setlayout,tile
bind=SUPER,s,setlayout,scroller
bind=SUPER,n,switch_layout

bind=ALT,e,set_proportion,1.0
bind=ALT,x,switch_proportion_preset,

bind=SUPER,Left,viewtoleft,0
bind=CTRL,Left,viewtoleft_have_client,0
bind=SUPER,Right,viewtoright,0
bind=CTRL,Right,viewtoright_have_client,0
bind=CTRL+SUPER,Left,tagtoleft,0
bind=CTRL+SUPER,Right,tagtoright,0

bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER,7,view,7,0
bind=SUPER,8,view,8,0
bind=SUPER,9,view,9,0

bind=SUPER+Shift,1,tag,1,0
bind=SUPER+Shift,2,tag,2,0
bind=SUPER+Shift,3,tag,3,0
bind=SUPER+Shift,4,tag,4,0
bind=SUPER+Shift,5,tag,5,0
bind=SUPER+Shift,6,tag,6,0
bind=SUPER+Shift,7,tag,7,0
bind=SUPER+Shift,8,tag,8,0
bind=SUPER+Shift,9,tag,9,0

bind=ALT+SHIFT,X,incgaps,1
bind=ALT+SHIFT,Z,incgaps,-1
bind=ALT+SHIFT,R,togglegaps

bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0

bind=CTRL+ALT,Up,resizewin,+0,-50
bind=CTRL+ALT,Down,resizewin,+0,+50
bind=CTRL+ALT,Left,resizewin,-50,+0
bind=CTRL+ALT,Right,resizewin,+50,+0

bind=NONE,XF86AudioRaiseVolume,spawn,qs -c noctalia-shell ipc call volume increase
bind=NONE,XF86AudioLowerVolume,spawn,qs -c noctalia-shell ipc call volume decrease
bind=NONE,XF86AudioMute,spawn,qs -c noctalia-shell ipc call volume muteOutput
bind=NONE,XF86AudioMicMute,spawn,qs -c noctalia-shell ipc call volume muteInput

bind=NONE,XF86AudioPlay,spawn,qs -c noctalia-shell ipc call media playPause
bind=NONE,XF86AudioNext,spawn,qs -c noctalia-shell ipc call media next
bind=NONE,XF86AudioPrev,spawn,qs -c noctalia-shell ipc call media previous

bind=NONE,XF86MonBrightnessUp,spawn,qs -c noctalia-shell ipc call brightness increase
bind=NONE,XF86MonBrightnessDown,spawn,qs -c noctalia-shell ipc call brightness decrease

bind=SUPER,c,spawn,qs -c noctalia-shell ipc call controlCenter toggle
bind=SUPER,w,spawn,qs -c noctalia-shell ipc call wallpaper random
bind=SUPER,b,spawn,qs -c noctalia-shell ipc call bluetooth togglePanel
bind=SUPER,o,spawn,qs -c noctalia-shell ipc call bar toggle
bind=SUPER+CTRL,n,spawn,qs -c noctalia-shell ipc call notifications toggleHistory
bind=SUPER+Shift,w,spawn,qs -c noctalia-shell ipc call wallpaper toggle

bind=SUPER+Shift,d,spawn,qs -c noctalia-shell ipc call darkMode toggle
bind=SUPER+Shift,n,spawn,qs -c noctalia-shell ipc call nightLight toggle
